{
  "name": "m402",
  "private": true,
  "version": "0.1.0",
  "description": "M402 (Market402) — the agent marketplace for BNB Chain. Discover, compare, and hire on-chain agents across rebalancing, grid trading, yield optimization, and health factor monitoring.",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.1.0",
    "typescript": "^5.5.4"
  },
  "packageManager": "pnpm@9.7.0",
  "engines": {
    "node": ">=20"
  }
}
